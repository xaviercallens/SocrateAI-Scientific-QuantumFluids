# Comparator setup (D6)

Goal: machine-check that each listed theorem in `QuantumFluidsShell`, `MadelungSplit`, `Duality`
states exactly what the generated challenge states, uses only `propext`, `Quot.sound`,
`Classical.choice`, and is accepted by the Lean kernel and by nanoda (second kernel). This closes
the F-NAME gap (statement equivalence) that the Lean audit alone does not.

## State (2026-09-19)
- Challenges generated by `scripts/make_comparator_challenges.py` for four solution modules
  (`QuantumFluidsShell` 11, `MadelungSplit` 5, `Duality` 9, `GPGalerkin` 7 theorems). Each challenge compiles with 0 errors and one
  `sorry` warning per listed theorem.
- Tools installed on the large disk: `/media/xavkal/3ada43de-fc4a-43bd-a9f8-cf396fd17033/home/xavkal/xavkal-tools/bin`
  (`landrun`, `lean4export`, `nanoda_bin`, `comparator`; log in `install.log`). `lean_src/.lake` is a symlink to
  `.../xavkal-tools/qf-lake` (Mathlib v4.34.0-rc2 + build), so nothing large lives on the root disk.
  `lean_src/lake-manifest.json` and `.lake/` are gitignored and regenerated by `lake update && lake exe cache get`.
- `lake build` of the four solution libraries succeeded (8770 jobs); this also validated the lakefile edits.
- **Comparator results:** all four modules accepted; see the table at the end of this file.

## Install
1. One privileged step, by the owner: make a user-owned directory on the large disk, e.g.
   `sudo install -d -o xavkal -g xavkal <mount>/xavkal-tools`.
   (`/dev/sda3`, 773 GB free, mounts under `/media/xavkal/3ada43de-...` but is a root-owned old system
   tree; any directory there needs sudo once.)
2. `TOOLS=<mount>/xavkal-tools bash scripts/install_comparator_tools.sh`
   builds Go (for landrun only), `landrun` (main), `lean4export` and `comparator` (tag v4.34.0-rc2),
   `nanoda_bin`. It never uses sudo and refuses a `TOOLS` on the root filesystem.
3. `export PATH=$TOOLS/bin:$PATH`

## Run (from `lean_src`, after `lake update` has produced a manifest)
```
python3 ../scripts/make_comparator_challenges.py
systemd-run --property=RestrictAddressFamilies=~AF_UNIX --user --pty -E PATH="$PATH" \
  --working-directory $(pwd) -- bash -c 'lake env comparator ComparatorChallenges/QuantumFluidsShell.json'
```
(repeat for `MadelungSplit.json`, `Duality.json`). Do NOT compile a Solution file before running
Comparator (its README: that can compromise the challenge). Do not run as a privileged user.

## Trust notes
- Generated challenges are the trusted step: review `ComparatorChallenges/*.lean` once.
- A Comparator pass says the solution proves the challenge's statement, not that the statement is
  the right physics. Statement adequacy remains a human audit (LeanMaster's 4th criterion).

## Results
| Module | Theorems | Result |
|---|---|---|
| QuantumFluidsShell | 11 | **accepted**: nanoda kernel accepts, Lean default kernel accepts ("Your solution is okay!"), 2 min 35 s |
| MadelungSplit | 5 | **accepted** (both kernels) |
| Duality | 9 | **accepted** (both kernels) |
| GPGalerkin | 13 | **accepted** (both kernels) |
| HeliumKinematics | 15 | **accepted** (both kernels) — slowest module by far, ~25 min against 2–3 for the others: nanoda works hard on the `HasDerivAt` dependency closures |
| BoseIntegral | 5 | **accepted** (both kernels) |
| ShellHamiltonian | 7 | **accepted** (both kernels) |
| DualLength | 12 | **accepted** (both kernels) |

All 109 listed theorems: accepted by the Lean kernel and by nanoda, permitted axioms only, run under `systemd-run --user` with `RestrictAddressFamilies=~AF_UNIX`.

Caveat on what a pass means: the challenge is *generated from* the solution by our script, so "same statement" is guaranteed by
construction. The independent content of a pass is (a) two kernels accept the exported proof terms, (b) only the permitted axioms are used,
(c) the exported closure is exactly the listed theorems and their dependencies. Whether each statement is the *right* statement
remains a human audit.
Also note: `lake build` compiled the solutions before Comparator ran. That is acceptable here because the solutions are our own files, but it is
outside the adversarial-solution threat model in Comparator's README.
